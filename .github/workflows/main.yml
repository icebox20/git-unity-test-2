name: Merge

on:
  push:
    branches: 
      - master
      - william

jobs:

  build:

    runs-on: windows-latest

    steps:

    - uses: actions/checkout@v2
        
    - name: Run a multi-line script
      run: |
        git fetch 
        git checkout 
        git checkout develop
        git branch
        git merge william
        git push
